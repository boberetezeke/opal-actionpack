require 'spec_helper'
require 'action_pack'

describe "ActionController::Base" do
  context "when using a controller with params" do
    it "should be true" do
      expect(nil).to eq(nil)
    end
  end
end
 
